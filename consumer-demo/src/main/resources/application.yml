server:
  port: 10301
spring:
  application:
    name: spring-cloud-consumer-demo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10101/eureka/
feign:
  compression:
    request:
      enabled: true
    response:
      enabled: true